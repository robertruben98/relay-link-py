{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"relay-link-py","text":"<p>A typed Python client for the Relay Protocol API \u2014 a fast crosschain bridge and swap protocol. Sync and async, pydantic v2 models, <code>py.typed</code>, no network needed to import.</p> <ul> <li>Base URL: <code>https://api.relay.link</code> (configurable)</li> <li>API reference: https://docs.relay.link/references/api/overview</li> <li>OpenAPI spec: https://api.relay.link/documentation/json</li> </ul>"},{"location":"#install","title":"Install","text":"<pre><code>pip install relay-link-py\n</code></pre> <p>Python 3.9+ is supported. The data layer needs only <code>httpx</code> and <code>pydantic</code>. Signing the transaction steps a quote returns is out of scope for the core client; install the optional extra if you want <code>web3</code> / <code>solders</code> available:</p> <pre><code>pip install \"relay-link-py[exec]\"\n</code></pre>"},{"location":"#quickstart","title":"Quickstart","text":"<pre><code>from relay_link import RelayClient, TradeType\n\nNATIVE = \"0x0000000000000000000000000000000000000000\"\n\nwith RelayClient() as client:\n    # List supported chains (no API key required)\n    chains = client.get_chains()\n    print(f\"{len(chains)} chains supported\")\n\n    # Get an executable quote: bridge 1 ETH from Base (8453) to Optimism (10)\n    quote = client.get_quote(\n        user=\"0x03508bb71268bba25ecacc8f620e01866650532c\",\n        origin_chain_id=8453,\n        destination_chain_id=10,\n        origin_currency=NATIVE,\n        destination_currency=NATIVE,\n        amount=\"1000000000000000000\",  # 1 ETH in wei\n        trade_type=TradeType.EXACT_INPUT,\n    )\n    for step in quote.steps:\n        print(step.kind, step.id, step.action)\n</code></pre>"},{"location":"#async","title":"Async","text":"<pre><code>import asyncio\nfrom relay_link import AsyncRelayClient, TradeType\n\nasync def main():\n    async with AsyncRelayClient() as client:\n        chains = await client.get_chains()\n        print(len(chains))\n\nasyncio.run(main())\n</code></pre>"},{"location":"#polling-execution-status","title":"Polling execution status","text":"<p>After you submit a step's transaction on-chain, poll its <code>requestId</code> until it reaches a terminal state (<code>success</code>, <code>failure</code>, or <code>refund</code>):</p> <pre><code>status = client.poll_status(request_id=\"0x...\", interval=2, timeout=120)\nprint(status.status, status.tx_hashes)\n</code></pre>"},{"location":"#configuration","title":"Configuration","text":"<pre><code>client = RelayClient(\n    base_url=\"https://api.relay.link\",  # configurable\n    api_key=\"your-key\",                 # optional; sent on every request\n    api_key_header=\"x-api-key\",         # configurable header name\n    timeout=30.0,\n    max_retries=3,                      # retries for 429 / 5xx responses\n    backoff_base=0.5,                   # exponential backoff base (seconds)\n)\n</code></pre> <p>Quotes are public and need no API key.</p>"},{"location":"#retries-and-errors","title":"Retries and errors","text":"<p><code>429</code> and transient <code>5xx</code> responses are retried up to <code>max_retries</code> times with exponential backoff, honoring a <code>Retry-After</code> header when present. The exception hierarchy:</p> <ul> <li><code>RelayError</code> \u2014 base class for everything this library raises.</li> <li><code>RelayAPIError</code> \u2014 non-2xx response; carries <code>status_code</code>, <code>message</code>, <code>body</code>.<ul> <li><code>RelayRateLimitError</code> \u2014 <code>429</code> after retries are exhausted; adds <code>retry_after</code>.</li> </ul> </li> <li><code>RelayConnectionError</code> \u2014 could not reach the API.</li> <li><code>RelayTimeoutError</code> \u2014 the request timed out.</li> </ul>"},{"location":"#api-surface","title":"API surface","text":"Method Endpoint Description <code>get_chains()</code> <code>GET /chains</code> List supported chains <code>get_quote(...)</code> <code>POST /quote/v2</code> Executable bridge/swap/call quote <code>get_price(...)</code> <code>POST /price</code> Non-executable price estimate <code>get_status(request_id=...)</code> <code>GET /intents/status/v3</code> Execution status <code>poll_status(request_id=...)</code> <code>GET /intents/status/v3</code> Poll to a terminal state <code>get_requests(...)</code> <code>GET /requests</code> List relay requests <code>get_token_price(address=..., chain_id=...)</code> <code>GET /currencies/token/price</code> Token USD price <p><code>AsyncRelayClient</code> exposes the same methods as awaitables.</p>"},{"location":"#development","title":"Development","text":"<pre><code>uv pip install -e \".[dev]\"\nruff check .\nmypy --strict src tests\npytest                  # unit tests (network mocked)\npytest -m integration   # one live test against GET /chains\n</code></pre>"},{"location":"#license","title":"License","text":"<p>MIT</p>"},{"location":"api/","title":"API reference","text":""},{"location":"api/#relay_link","title":"<code>relay_link</code>","text":"<p>relay-link-py: a Python client for the Relay Protocol API (relay.link).</p>"},{"location":"api/#relay_link--quickstart","title":"Quickstart","text":"<p>from relay_link import RelayClient, TradeType client = RelayClient() chains = client.get_chains()</p>"},{"location":"api/#relay_link.AsyncRelayClient","title":"<code>AsyncRelayClient</code>","text":"<p>               Bases: <code>_BaseRelayClient</code></p> <p>Asynchronous client for the Relay Protocol API.</p> <p>Use it as an async context manager so the underlying HTTP connection is closed automatically:</p> <p>async with AsyncRelayClient() as client: ...     chains = await client.get_chains()</p> <p>It mirrors :class:<code>RelayClient</code> exactly, with every method returning an awaitable. The same retry and error-handling behavior applies.</p>"},{"location":"api/#relay_link.AsyncRelayClient.__init__","title":"<code>__init__(base_url=DEFAULT_BASE_URL, api_key=None, api_key_header='x-api-key', timeout=30.0, max_retries=DEFAULT_MAX_RETRIES, backoff_base=DEFAULT_BACKOFF_BASE, http_client=None)</code>","text":"<p>Create an asynchronous client.</p> <p>Parameters:</p> Name Type Description Default <code>base_url</code> <code>str</code> <p>Base URL of the Relay API. A trailing slash is stripped.</p> <code>DEFAULT_BASE_URL</code> <code>api_key</code> <code>Optional[str]</code> <p>Optional API key sent on every request.</p> <code>None</code> <code>api_key_header</code> <code>str</code> <p>Header name to send <code>api_key</code> under.</p> <code>'x-api-key'</code> <code>timeout</code> <code>float</code> <p>Per-request timeout in seconds (used only when this client creates its own HTTP client).</p> <code>30.0</code> <code>max_retries</code> <code>int</code> <p>Maximum retries for 429 and transient 5xx responses.</p> <code>DEFAULT_MAX_RETRIES</code> <code>backoff_base</code> <code>float</code> <p>Base delay in seconds for exponential backoff.</p> <code>DEFAULT_BACKOFF_BASE</code> <code>http_client</code> <code>Optional[AsyncClient]</code> <p>An existing :class:<code>httpx.AsyncClient</code> to use. When given, the caller owns its lifecycle and :meth:<code>aclose</code> will not close it; otherwise the client creates and owns one.</p> <code>None</code>"},{"location":"api/#relay_link.AsyncRelayClient.__aenter__","title":"<code>__aenter__()</code>  <code>async</code>","text":"<p>Enter the async context manager, returning this client.</p>"},{"location":"api/#relay_link.AsyncRelayClient.__aexit__","title":"<code>__aexit__(exc_type, exc, tb)</code>  <code>async</code>","text":"<p>Exit the async context manager, closing the client.</p>"},{"location":"api/#relay_link.AsyncRelayClient.aclose","title":"<code>aclose()</code>  <code>async</code>","text":"<p>Close the underlying async HTTP client if this instance created it.</p> <p>A no-op when an <code>http_client</code> was supplied to the constructor.</p>"},{"location":"api/#relay_link.AsyncRelayClient.get_chains","title":"<code>get_chains(include_chains=None)</code>  <code>async</code>","text":"<p>List the chains supported by Relay (<code>GET /chains</code>).</p> <p>Parameters:</p> Name Type Description Default <code>include_chains</code> <code>Optional[str]</code> <p>Optional comma-separated chain IDs to restrict the response to (e.g. <code>\"1,10,8453\"</code>).</p> <code>None</code> <p>Returns:</p> Type Description <code>list[Chain]</code> <p>The list of supported :class:<code>~relay_link.Chain</code> objects.</p> <p>Raises:</p> Type Description <code>RelayAPIError</code> <p>The API returned a non-2xx response.</p> <code>RelayConnectionError</code> <p>The API could not be reached.</p> <code>RelayTimeoutError</code> <p>The request timed out.</p>"},{"location":"api/#relay_link.AsyncRelayClient.get_quote","title":"<code>get_quote(*, user, origin_chain_id, destination_chain_id, origin_currency, destination_currency, amount, trade_type, **extra)</code>  <code>async</code>","text":"<p>Get an executable quote for a bridge/swap/call (<code>POST /quote/v2</code>).</p> <p>Parameters:</p> Name Type Description Default <code>user</code> <code>str</code> <p>Address depositing funds and submitting transactions.</p> required <code>origin_chain_id</code> <code>int</code> <p>Chain ID to bridge/swap from.</p> required <code>destination_chain_id</code> <code>int</code> <p>Chain ID to bridge/swap to.</p> required <code>origin_currency</code> <code>str</code> <p>Origin token address (zero = native).</p> required <code>destination_currency</code> <code>str</code> <p>Destination token address (zero = native).</p> required <code>amount</code> <code>str</code> <p>Amount in the currency's smallest unit (e.g. wei), as a string.</p> required <code>trade_type</code> <code>Union[str, Any]</code> <p>A :class:<code>~relay_link.TradeType</code> (or its string value) deciding whether <code>amount</code> is the input or desired output.</p> required <code>**extra</code> <code>Any</code> <p>Any additional :class:<code>~relay_link.QuoteRequest</code> fields, e.g. <code>recipient</code>, <code>slippage_tolerance</code>, <code>app_fees</code>.</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>A</code> <code>Quote</code> <p>class:<code>~relay_link.Quote</code> with the steps to execute and the fee</p> <code>Quote</code> <p>breakdown.</p> <p>Raises:</p> Type Description <code>RelayAPIError</code> <p>The API rejected the request (e.g. an invalid route).</p> <code>RelayRateLimitError</code> <p>Rate limited after retries were exhausted.</p> <code>RelayConnectionError</code> <p>The API could not be reached.</p> <code>RelayTimeoutError</code> <p>The request timed out.</p> Example <p>async with AsyncRelayClient() as client: ...     quote = await client.get_quote( ...         user=\"0x03508bb71268bba25ecacc8f620e01866650532c\", ...         origin_chain_id=8453, ...         destination_chain_id=10, ...         origin_currency=\"0x0000000000000000000000000000000000000000\", ...         destination_currency=\"0x0000000000000000000000000000000000000000\", ...         amount=\"1000000000000000000\", ...         trade_type=TradeType.EXACT_INPUT, ...     )</p>"},{"location":"api/#relay_link.AsyncRelayClient.get_price","title":"<code>get_price(*, origin_chain_id, destination_chain_id, origin_currency, destination_currency, amount, trade_type, user=None, **extra)</code>  <code>async</code>","text":"<p>Get a non-executable price estimate (<code>POST /price</code>).</p> <p>Like :meth:<code>get_quote</code> but returns only fees and swap details, with no executable steps.</p> <p>Parameters:</p> Name Type Description Default <code>origin_chain_id</code> <code>int</code> <p>Chain ID to bridge/swap from.</p> required <code>destination_chain_id</code> <code>int</code> <p>Chain ID to bridge/swap to.</p> required <code>origin_currency</code> <code>str</code> <p>Origin token address (zero = native).</p> required <code>destination_currency</code> <code>str</code> <p>Destination token address (zero = native).</p> required <code>amount</code> <code>str</code> <p>Amount in the currency's smallest unit, as a string.</p> required <code>trade_type</code> <code>Union[str, Any]</code> <p>A :class:<code>~relay_link.TradeType</code> (or its string value).</p> required <code>user</code> <code>Optional[str]</code> <p>Optional depositing address; not required for a price-only call.</p> <code>None</code> <code>**extra</code> <code>Any</code> <p>Any additional :class:<code>~relay_link.PriceRequest</code> fields.</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>A</code> <code>Price</code> <p>class:<code>~relay_link.Price</code> with the estimated fee breakdown and</p> <code>Price</code> <p>swap details.</p> <p>Raises:</p> Type Description <code>RelayAPIError</code> <p>The API rejected the request.</p> <code>RelayRateLimitError</code> <p>Rate limited after retries were exhausted.</p> <code>RelayConnectionError</code> <p>The API could not be reached.</p> <code>RelayTimeoutError</code> <p>The request timed out.</p>"},{"location":"api/#relay_link.AsyncRelayClient.get_status","title":"<code>get_status(*, request_id)</code>  <code>async</code>","text":"<p>Get the execution status of a request (<code>GET /intents/status/v3</code>).</p> <p>Parameters:</p> Name Type Description Default <code>request_id</code> <code>str</code> <p>The request identifier from a quote step's <code>request_id</code>.</p> required <p>Returns:</p> Name Type Description <code>A</code> <code>TransactionStatus</code> <p>class:<code>~relay_link.TransactionStatus</code> snapshot.</p> <p>Raises:</p> Type Description <code>RelayAPIError</code> <p>The API returned a non-2xx response.</p> <code>RelayConnectionError</code> <p>The API could not be reached.</p> <code>RelayTimeoutError</code> <p>The request timed out.</p>"},{"location":"api/#relay_link.AsyncRelayClient.get_requests","title":"<code>get_requests(*, user=None, hash=None, origin_chain_id=None, destination_chain_id=None, limit=None, continuation=None)</code>  <code>async</code>","text":"<p>List relay requests (<code>GET /requests</code>).</p> <p>All filters are optional; omitted ones are not sent. Page through results by passing the previous response's <code>continuation</code> back in.</p> <p>Parameters:</p> Name Type Description Default <code>user</code> <code>Optional[str]</code> <p>Filter to requests initiated by this address.</p> <code>None</code> <code>hash</code> <code>Optional[str]</code> <p>Filter to a specific transaction hash.</p> <code>None</code> <code>origin_chain_id</code> <code>Optional[int]</code> <p>Filter by origin chain ID.</p> <code>None</code> <code>destination_chain_id</code> <code>Optional[int]</code> <p>Filter by destination chain ID.</p> <code>None</code> <code>limit</code> <code>Optional[int]</code> <p>Maximum number of records to return.</p> <code>None</code> <code>continuation</code> <code>Optional[str]</code> <p>Pagination cursor from a previous response.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>A</code> <code>RequestsResponse</code> <p>class:<code>~relay_link.RequestsResponse</code> page of records plus the</p> <code>RequestsResponse</code> <p>next <code>continuation</code> cursor.</p> <p>Raises:</p> Type Description <code>RelayAPIError</code> <p>The API returned a non-2xx response.</p> <code>RelayConnectionError</code> <p>The API could not be reached.</p> <code>RelayTimeoutError</code> <p>The request timed out.</p>"},{"location":"api/#relay_link.AsyncRelayClient.get_token_price","title":"<code>get_token_price(*, address, chain_id)</code>  <code>async</code>","text":"<p>Get a token's USD price (<code>GET /currencies/token/price</code>).</p> <p>Parameters:</p> Name Type Description Default <code>address</code> <code>str</code> <p>Token contract address.</p> required <code>chain_id</code> <code>int</code> <p>Chain ID the token lives on.</p> required <p>Returns:</p> Name Type Description <code>A</code> <code>TokenPrice</code> <p>class:<code>~relay_link.TokenPrice</code> with the USD price.</p> <p>Raises:</p> Type Description <code>RelayAPIError</code> <p>The API returned a non-2xx response.</p> <code>RelayConnectionError</code> <p>The API could not be reached.</p> <code>RelayTimeoutError</code> <p>The request timed out.</p>"},{"location":"api/#relay_link.AsyncRelayClient.poll_status","title":"<code>poll_status(*, request_id, interval=2.0, timeout=120.0)</code>  <code>async</code>","text":"<p>Poll :meth:<code>get_status</code> until the request reaches a terminal state.</p> <p>Repeatedly calls :meth:<code>get_status</code>, awaiting <code>interval</code> seconds between polls, until <code>status</code> is one of :data:<code>~relay_link.TERMINAL_STATUSES</code> (<code>success</code>, <code>failure</code> or <code>refund</code>).</p> <p>Parameters:</p> Name Type Description Default <code>request_id</code> <code>str</code> <p>The request identifier to poll.</p> required <code>interval</code> <code>float</code> <p>Seconds to wait between polls.</p> <code>2.0</code> <code>timeout</code> <code>float</code> <p>Maximum total seconds to poll before giving up.</p> <code>120.0</code> <p>Returns:</p> Type Description <code>TransactionStatus</code> <p>The terminal :class:<code>~relay_link.TransactionStatus</code>.</p> <p>Raises:</p> Type Description <code>RelayError</code> <p>The timeout elapsed before reaching a terminal state.</p> <code>RelayAPIError</code> <p>A status request returned a non-2xx response.</p> <code>RelayConnectionError</code> <p>The API could not be reached.</p> <code>RelayTimeoutError</code> <p>A status request timed out.</p>"},{"location":"api/#relay_link.RelayClient","title":"<code>RelayClient</code>","text":"<p>               Bases: <code>_BaseRelayClient</code></p> <p>Synchronous client for the Relay Protocol API.</p> <p>Use it as a context manager so the underlying HTTP connection is closed automatically:</p> <p>with RelayClient() as client: ...     chains = client.get_chains()</p> <p>All methods raise a :class:<code>RelayError</code> subclass on failure (see the module's exception hierarchy). 429 and transient 5xx responses are retried transparently with exponential backoff.</p>"},{"location":"api/#relay_link.RelayClient.__init__","title":"<code>__init__(base_url=DEFAULT_BASE_URL, api_key=None, api_key_header='x-api-key', timeout=30.0, max_retries=DEFAULT_MAX_RETRIES, backoff_base=DEFAULT_BACKOFF_BASE, http_client=None)</code>","text":"<p>Create a synchronous client.</p> <p>Parameters:</p> Name Type Description Default <code>base_url</code> <code>str</code> <p>Base URL of the Relay API. A trailing slash is stripped.</p> <code>DEFAULT_BASE_URL</code> <code>api_key</code> <code>Optional[str]</code> <p>Optional API key sent on every request. Quotes are public and need no key.</p> <code>None</code> <code>api_key_header</code> <code>str</code> <p>Header name to send <code>api_key</code> under.</p> <code>'x-api-key'</code> <code>timeout</code> <code>float</code> <p>Per-request timeout in seconds (used only when this client creates its own HTTP client).</p> <code>30.0</code> <code>max_retries</code> <code>int</code> <p>Maximum retries for 429 and transient 5xx responses.</p> <code>DEFAULT_MAX_RETRIES</code> <code>backoff_base</code> <code>float</code> <p>Base delay in seconds for exponential backoff between retries.</p> <code>DEFAULT_BACKOFF_BASE</code> <code>http_client</code> <code>Optional[Client]</code> <p>An existing :class:<code>httpx.Client</code> to use. When given, the caller owns its lifecycle and :meth:<code>close</code> will not close it; otherwise the client creates and owns one.</p> <code>None</code>"},{"location":"api/#relay_link.RelayClient.__enter__","title":"<code>__enter__()</code>","text":"<p>Enter the context manager, returning this client.</p>"},{"location":"api/#relay_link.RelayClient.__exit__","title":"<code>__exit__(exc_type, exc, tb)</code>","text":"<p>Exit the context manager, closing the client.</p>"},{"location":"api/#relay_link.RelayClient.close","title":"<code>close()</code>","text":"<p>Close the underlying HTTP client if this instance created it.</p> <p>A no-op when an <code>http_client</code> was supplied to the constructor, since the caller owns that client's lifecycle.</p>"},{"location":"api/#relay_link.RelayClient.get_chains","title":"<code>get_chains(include_chains=None)</code>","text":"<p>List the chains supported by Relay (<code>GET /chains</code>).</p> <p>Parameters:</p> Name Type Description Default <code>include_chains</code> <code>Optional[str]</code> <p>Optional comma-separated chain IDs to restrict the response to (e.g. <code>\"1,10,8453\"</code>).</p> <code>None</code> <p>Returns:</p> Type Description <code>list[Chain]</code> <p>The list of supported :class:<code>~relay_link.Chain</code> objects.</p> <p>Raises:</p> Type Description <code>RelayAPIError</code> <p>The API returned a non-2xx response.</p> <code>RelayConnectionError</code> <p>The API could not be reached.</p> <code>RelayTimeoutError</code> <p>The request timed out.</p>"},{"location":"api/#relay_link.RelayClient.get_quote","title":"<code>get_quote(*, user, origin_chain_id, destination_chain_id, origin_currency, destination_currency, amount, trade_type, **extra)</code>","text":"<p>Get an executable quote for a bridge/swap/call (<code>POST /quote/v2</code>).</p> <p>Parameters:</p> Name Type Description Default <code>user</code> <code>str</code> <p>Address depositing funds and submitting transactions.</p> required <code>origin_chain_id</code> <code>int</code> <p>Chain ID to bridge/swap from.</p> required <code>destination_chain_id</code> <code>int</code> <p>Chain ID to bridge/swap to.</p> required <code>origin_currency</code> <code>str</code> <p>Origin token address (the zero address for the native gas currency).</p> required <code>destination_currency</code> <code>str</code> <p>Destination token address (zero = native).</p> required <code>amount</code> <code>str</code> <p>Amount in the currency's smallest unit (e.g. wei), as a string.</p> required <code>trade_type</code> <code>Union[str, Any]</code> <p>A :class:<code>~relay_link.TradeType</code> (or its string value) deciding whether <code>amount</code> is the input or desired output.</p> required <code>**extra</code> <code>Any</code> <p>Any additional :class:<code>~relay_link.QuoteRequest</code> fields, e.g. <code>recipient</code>, <code>slippage_tolerance</code>, <code>app_fees</code>.</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>A</code> <code>Quote</code> <p>class:<code>~relay_link.Quote</code> with the steps to execute and the fee</p> <code>Quote</code> <p>breakdown.</p> <p>Raises:</p> Type Description <code>RelayAPIError</code> <p>The API rejected the request (e.g. an invalid route).</p> <code>RelayRateLimitError</code> <p>Rate limited after retries were exhausted.</p> <code>RelayConnectionError</code> <p>The API could not be reached.</p> <code>RelayTimeoutError</code> <p>The request timed out.</p> Example <p>with RelayClient() as client: ...     quote = client.get_quote( ...         user=\"0x03508bb71268bba25ecacc8f620e01866650532c\", ...         origin_chain_id=8453, ...         destination_chain_id=10, ...         origin_currency=\"0x0000000000000000000000000000000000000000\", ...         destination_currency=\"0x0000000000000000000000000000000000000000\", ...         amount=\"1000000000000000000\",  # 1 ETH in wei ...         trade_type=TradeType.EXACT_INPUT, ...     ) for step in quote.steps: ...     print(step.kind, step.id)</p>"},{"location":"api/#relay_link.RelayClient.get_price","title":"<code>get_price(*, origin_chain_id, destination_chain_id, origin_currency, destination_currency, amount, trade_type, user=None, **extra)</code>","text":"<p>Get a non-executable price estimate (<code>POST /price</code>).</p> <p>Like :meth:<code>get_quote</code> but returns only fees and swap details, with no executable steps \u2014 useful for display and comparison.</p> <p>Parameters:</p> Name Type Description Default <code>origin_chain_id</code> <code>int</code> <p>Chain ID to bridge/swap from.</p> required <code>destination_chain_id</code> <code>int</code> <p>Chain ID to bridge/swap to.</p> required <code>origin_currency</code> <code>str</code> <p>Origin token address (zero = native).</p> required <code>destination_currency</code> <code>str</code> <p>Destination token address (zero = native).</p> required <code>amount</code> <code>str</code> <p>Amount in the currency's smallest unit, as a string.</p> required <code>trade_type</code> <code>Union[str, Any]</code> <p>A :class:<code>~relay_link.TradeType</code> (or its string value).</p> required <code>user</code> <code>Optional[str]</code> <p>Optional depositing address; not required for a price-only call.</p> <code>None</code> <code>**extra</code> <code>Any</code> <p>Any additional :class:<code>~relay_link.PriceRequest</code> fields.</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>A</code> <code>Price</code> <p>class:<code>~relay_link.Price</code> with the estimated fee breakdown and</p> <code>Price</code> <p>swap details.</p> <p>Raises:</p> Type Description <code>RelayAPIError</code> <p>The API rejected the request.</p> <code>RelayRateLimitError</code> <p>Rate limited after retries were exhausted.</p> <code>RelayConnectionError</code> <p>The API could not be reached.</p> <code>RelayTimeoutError</code> <p>The request timed out.</p>"},{"location":"api/#relay_link.RelayClient.get_status","title":"<code>get_status(*, request_id)</code>","text":"<p>Get the execution status of a request (<code>GET /intents/status/v3</code>).</p> <p>Parameters:</p> Name Type Description Default <code>request_id</code> <code>str</code> <p>The request identifier from a quote step's <code>request_id</code>.</p> required <p>Returns:</p> Name Type Description <code>A</code> <code>TransactionStatus</code> <p>class:<code>~relay_link.TransactionStatus</code> snapshot.</p> <p>Raises:</p> Type Description <code>RelayAPIError</code> <p>The API returned a non-2xx response.</p> <code>RelayConnectionError</code> <p>The API could not be reached.</p> <code>RelayTimeoutError</code> <p>The request timed out.</p>"},{"location":"api/#relay_link.RelayClient.get_requests","title":"<code>get_requests(*, user=None, hash=None, origin_chain_id=None, destination_chain_id=None, limit=None, continuation=None)</code>","text":"<p>List relay requests (<code>GET /requests</code>).</p> <p>All filters are optional; omitted ones are not sent. Page through results by passing the previous response's <code>continuation</code> back in.</p> <p>Parameters:</p> Name Type Description Default <code>user</code> <code>Optional[str]</code> <p>Filter to requests initiated by this address.</p> <code>None</code> <code>hash</code> <code>Optional[str]</code> <p>Filter to a specific transaction hash.</p> <code>None</code> <code>origin_chain_id</code> <code>Optional[int]</code> <p>Filter by origin chain ID.</p> <code>None</code> <code>destination_chain_id</code> <code>Optional[int]</code> <p>Filter by destination chain ID.</p> <code>None</code> <code>limit</code> <code>Optional[int]</code> <p>Maximum number of records to return.</p> <code>None</code> <code>continuation</code> <code>Optional[str]</code> <p>Pagination cursor from a previous response.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>A</code> <code>RequestsResponse</code> <p>class:<code>~relay_link.RequestsResponse</code> page of records plus the</p> <code>RequestsResponse</code> <p>next <code>continuation</code> cursor.</p> <p>Raises:</p> Type Description <code>RelayAPIError</code> <p>The API returned a non-2xx response.</p> <code>RelayConnectionError</code> <p>The API could not be reached.</p> <code>RelayTimeoutError</code> <p>The request timed out.</p>"},{"location":"api/#relay_link.RelayClient.get_token_price","title":"<code>get_token_price(*, address, chain_id)</code>","text":"<p>Get a token's USD price (<code>GET /currencies/token/price</code>).</p> <p>Parameters:</p> Name Type Description Default <code>address</code> <code>str</code> <p>Token contract address.</p> required <code>chain_id</code> <code>int</code> <p>Chain ID the token lives on.</p> required <p>Returns:</p> Name Type Description <code>A</code> <code>TokenPrice</code> <p>class:<code>~relay_link.TokenPrice</code> with the USD price.</p> <p>Raises:</p> Type Description <code>RelayAPIError</code> <p>The API returned a non-2xx response.</p> <code>RelayConnectionError</code> <p>The API could not be reached.</p> <code>RelayTimeoutError</code> <p>The request timed out.</p>"},{"location":"api/#relay_link.RelayClient.poll_status","title":"<code>poll_status(*, request_id, interval=2.0, timeout=120.0)</code>","text":"<p>Poll :meth:<code>get_status</code> until the request reaches a terminal state.</p> <p>Repeatedly calls :meth:<code>get_status</code>, sleeping <code>interval</code> seconds between polls, until <code>status</code> is one of :data:<code>~relay_link.TERMINAL_STATUSES</code> (<code>success</code>, <code>failure</code> or <code>refund</code>).</p> <p>Parameters:</p> Name Type Description Default <code>request_id</code> <code>str</code> <p>The request identifier to poll.</p> required <code>interval</code> <code>float</code> <p>Seconds to wait between polls.</p> <code>2.0</code> <code>timeout</code> <code>float</code> <p>Maximum total seconds to poll before giving up.</p> <code>120.0</code> <p>Returns:</p> Type Description <code>TransactionStatus</code> <p>The terminal :class:<code>~relay_link.TransactionStatus</code>.</p> <p>Raises:</p> Type Description <code>RelayError</code> <p>The timeout elapsed before reaching a terminal state.</p> <code>RelayAPIError</code> <p>A status request returned a non-2xx response.</p> <code>RelayConnectionError</code> <p>The API could not be reached.</p> <code>RelayTimeoutError</code> <p>A status request timed out.</p>"},{"location":"api/#relay_link.RelayAPIError","title":"<code>RelayAPIError</code>","text":"<p>               Bases: <code>RelayError</code></p> <p>Raised when the Relay API returns a non-2xx response.</p> <p>Attributes:</p> Name Type Description <code>status_code</code> <p>The HTTP status code of the response.</p> <code>message</code> <p>A human-readable message, extracted from the response body when possible (the <code>message</code> or <code>error</code> field), else the HTTP reason phrase.</p> <code>body</code> <p>The parsed JSON body, or <code>None</code> if it was not valid JSON.</p>"},{"location":"api/#relay_link.RelayAPIError.__init__","title":"<code>__init__(status_code, message, body=None)</code>","text":"<p>Build an API error.</p> <p>Parameters:</p> Name Type Description Default <code>status_code</code> <code>int</code> <p>HTTP status code of the failing response.</p> required <code>message</code> <code>str</code> <p>Human-readable error message.</p> required <code>body</code> <code>Optional[Any]</code> <p>Parsed response body, if any.</p> <code>None</code>"},{"location":"api/#relay_link.RelayAPIError.from_response","title":"<code>from_response(response)</code>  <code>classmethod</code>","text":"<p>Construct an error from an httpx response.</p> <p>Parameters:</p> Name Type Description Default <code>response</code> <code>Response</code> <p>The non-2xx response to wrap.</p> required <p>Returns:</p> Type Description <code>RelayAPIError</code> <p>An instance of <code>cls</code> whose <code>message</code> is taken from the body's</p> <code>RelayAPIError</code> <p><code>message</code>/<code>error</code> field when present, otherwise the HTTP reason</p> <code>RelayAPIError</code> <p>phrase. <code>body</code> holds the parsed JSON, or <code>None</code> if the body is</p> <code>RelayAPIError</code> <p>not valid JSON.</p>"},{"location":"api/#relay_link.RelayConnectionError","title":"<code>RelayConnectionError</code>","text":"<p>               Bases: <code>RelayError</code></p> <p>Raised when the request fails to reach the API.</p> <p>Wraps a non-timeout :class:<code>httpx.TransportError</code> (e.g. a refused or reset connection, or DNS failure). The original exception is available via <code>__cause__</code>.</p>"},{"location":"api/#relay_link.RelayError","title":"<code>RelayError</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Base class for every error raised by this library.</p> <p>Catch this to handle any failure \u2014 API errors, rate limiting, connection failures and timeouts all subclass it.</p>"},{"location":"api/#relay_link.RelayRateLimitError","title":"<code>RelayRateLimitError</code>","text":"<p>               Bases: <code>RelayAPIError</code></p> <p>Raised on HTTP 429 once retries are exhausted.</p> <p>A subclass of :class:<code>RelayAPIError</code>, so <code>status_code</code> (always 429), <code>message</code> and <code>body</code> are available too.</p> <p>Attributes:</p> Name Type Description <code>retry_after</code> <p>The server-advised wait in seconds parsed from the <code>Retry-After</code> header, or <code>None</code> when the header was absent or unparseable.</p>"},{"location":"api/#relay_link.RelayRateLimitError.__init__","title":"<code>__init__(status_code, message, body=None, retry_after=None)</code>","text":"<p>Build a rate-limit error.</p> <p>Parameters:</p> Name Type Description Default <code>status_code</code> <code>int</code> <p>HTTP status code (429).</p> required <code>message</code> <code>str</code> <p>Human-readable error message.</p> required <code>body</code> <code>Optional[Any]</code> <p>Parsed response body, if any.</p> <code>None</code> <code>retry_after</code> <code>Optional[float]</code> <p>Server-advised wait in seconds, if available.</p> <code>None</code>"},{"location":"api/#relay_link.RelayTimeoutError","title":"<code>RelayTimeoutError</code>","text":"<p>               Bases: <code>RelayError</code></p> <p>Raised when the request to the API times out.</p> <p>Wraps an :class:<code>httpx.TimeoutException</code> (connect, read, write or pool timeout). The original exception is available via <code>__cause__</code>.</p>"},{"location":"api/#relay_link.Chain","title":"<code>Chain</code>","text":"<p>               Bases: <code>RelayModel</code></p> <p>A chain supported by Relay (<code>GET /chains</code>).</p> <p>Only <code>id</code> is guaranteed present; everything else is optional because the API tailors the payload to the chain and request.</p>"},{"location":"api/#relay_link.ChainCurrency","title":"<code>ChainCurrency</code>","text":"<p>               Bases: <code>RelayModel</code></p> <p>The native currency of a :class:<code>Chain</code>.</p>"},{"location":"api/#relay_link.ChainsResponse","title":"<code>ChainsResponse</code>","text":"<p>               Bases: <code>RelayModel</code></p> <p>Wrapper for the <code>GET /chains</code> response body.</p>"},{"location":"api/#relay_link.Currency","title":"<code>Currency</code>","text":"<p>               Bases: <code>RelayModel</code></p> <p>A token on a specific chain, as returned inside fees and quote details.</p>"},{"location":"api/#relay_link.CurrencyMetadata","title":"<code>CurrencyMetadata</code>","text":"<p>               Bases: <code>RelayModel</code></p> <p>Display and classification metadata for a :class:<code>Currency</code>.</p>"},{"location":"api/#relay_link.Fee","title":"<code>Fee</code>","text":"<p>               Bases: <code>RelayModel</code></p> <p>A single fee bucket: the currency charged plus its amounts.</p> <p><code>amount</code> is the raw value in the currency's smallest unit; <code>amount_formatted</code> is the same value scaled by <code>decimals</code>; and <code>amount_usd</code> is the USD equivalent. Some buckets (e.g. <code>relayer_service</code>) can be negative, representing a network reward rather than a charge.</p>"},{"location":"api/#relay_link.FeeBreakdown","title":"<code>FeeBreakdown</code>","text":"<p>               Bases: <code>RelayModel</code></p> <p>The set of fee buckets returned with a :class:<code>Quote</code> or :class:<code>Price</code>.</p> <p>Every bucket is optional; the API only populates the ones relevant to the route. <code>relayer</code> is the sum of <code>relayer_gas</code> and <code>relayer_service</code>.</p>"},{"location":"api/#relay_link.Price","title":"<code>Price</code>","text":"<p>               Bases: <code>RelayModel</code></p> <p>A non-executable price estimate (<code>POST /price</code>).</p> <p>Returned by :meth:<code>relay_link.RelayClient.get_price</code>. Unlike :class:<code>Quote</code> it contains no executable steps \u2014 only the fee breakdown and swap details.</p>"},{"location":"api/#relay_link.Quote","title":"<code>Quote</code>","text":"<p>               Bases: <code>RelayModel</code></p> <p>An executable quote: the steps to run plus the fee breakdown.</p> <p>Returned by :meth:<code>relay_link.RelayClient.get_quote</code>. <code>details</code>, <code>fee_sponsorship</code> and <code>protocol</code> are intentionally loose dicts \u2014 they are large, optional, and evolve frequently, so they are passed through verbatim rather than modeled exhaustively.</p>"},{"location":"api/#relay_link.RelayRequest","title":"<code>RelayRequest</code>","text":"<p>               Bases: <code>RelayModel</code></p> <p>A single relay request record (<code>GET /requests</code>).</p>"},{"location":"api/#relay_link.RequestsResponse","title":"<code>RequestsResponse</code>","text":"<p>               Bases: <code>RelayModel</code></p> <p>Paginated response for <code>GET /requests</code>.</p> <p>Returned by :meth:<code>relay_link.RelayClient.get_requests</code>. Pass <code>continuation</code> back into the next call to page through results.</p>"},{"location":"api/#relay_link.Step","title":"<code>Step</code>","text":"<p>               Bases: <code>RelayModel</code></p> <p>One step (signature or transaction) required to execute a quote.</p>"},{"location":"api/#relay_link.StepItem","title":"<code>StepItem</code>","text":"<p>               Bases: <code>RelayModel</code></p> <p>A single signature or transaction within a :class:<code>Step</code>.</p> <p>A step may bundle several items of the same kind that can be executed together. <code>data</code> holds the kind-specific payload to sign or submit.</p>"},{"location":"api/#relay_link.TokenPrice","title":"<code>TokenPrice</code>","text":"<p>               Bases: <code>RelayModel</code></p> <p>Token price in USD (<code>GET /currencies/token/price</code>).</p> <p>Returned by :meth:<code>relay_link.RelayClient.get_token_price</code>.</p>"},{"location":"api/#relay_link.TradeType","title":"<code>TradeType</code>","text":"<p>               Bases: <code>str</code>, <code>Enum</code></p> <p>Whether <code>amount</code> is the input or the desired output of the swap.</p>"},{"location":"api/#relay_link.TransactionStatus","title":"<code>TransactionStatus</code>","text":"<p>               Bases: <code>RelayModel</code></p> <p>Execution status of a relay request (<code>GET /intents/status/v3</code>).</p> <p>Returned by :meth:<code>relay_link.RelayClient.get_status</code>. <code>status</code> reaches a terminal value of <code>success</code>, <code>failure</code> or <code>refund</code> (see :data:<code>relay_link.TERMINAL_STATUSES</code>); intermediate values include <code>waiting</code>, <code>pending</code>, <code>depositing</code> and <code>submitted</code>.</p>"},{"location":"api/#relay_link.PriceRequest","title":"<code>PriceRequest</code>","text":"<p>               Bases: <code>_RequestBase</code></p> <p>Body for <code>POST /price</code> (non-executable price estimate).</p> <p>Like :class:<code>QuoteRequest</code> but <code>user</code> is optional, and the response contains no executable steps. Usually built for you by :meth:<code>relay_link.RelayClient.get_price</code>.</p>"},{"location":"api/#relay_link.QuoteRequest","title":"<code>QuoteRequest</code>","text":"<p>               Bases: <code>_RequestBase</code></p> <p>Body for <code>POST /quote/v2</code> (and the deprecated <code>POST /quote</code>).</p> <p>The first seven fields are required; the rest are optional tuning knobs that are only sent when set. Usually built for you by :meth:<code>relay_link.RelayClient.get_quote</code>, but exposed for callers that want to construct and reuse a request explicitly.</p>"}]}